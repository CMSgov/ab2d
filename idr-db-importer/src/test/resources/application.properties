# --- DB (values don't matter, we mock DriverManager)
spring.datasource.url=jdbc:postgresql://host:5432/db
spring.datasource.username=user
spring.datasource.password=pass

# --- S3
app.awsEndpoint="http://127.0.0.1:8001"
app.awsRegion=us-east-1
app.s3.bucket=ab2dimportfroms3test-bucket

# --- Import target
coverage.import.schema=v3
coverage.import.table=coverage_v3
